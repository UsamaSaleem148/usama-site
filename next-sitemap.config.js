/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.usama.solutions', // 👈 your live domain
  generateRobotsTxt: true, // auto-generate robots.txt
  sitemapSize: 7000, // optional (splits large sitemaps)
}
